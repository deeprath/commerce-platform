# Vite inlines VITE_* at BUILD time. Rebuild to change them.
# Where the browser fetches product media (a CDN in prod; MinIO locally).
VITE_MEDIA_BASE_URL=http://localhost:9000
# Dev-server proxy target for /api (see vite.config.ts).
VITE_BFF_TARGET=http://localhost:8088
